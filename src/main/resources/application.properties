# Persistence Properties
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=sa #this was not enabled

# Create the database in the memory
spring.datasource.url=jdbc:h2:mem:testdb

#everything until this line was enabled for default example

#OracleJDBC
#spring.datasource.url=jdbc:oracle:thin:@multilogus.dynns.com:1521:xe
#spring.datasource.username=ducks
#spring.datasource.password=qwerty
#spring.jpa.hibernate.ddl-auto=create
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.Oracle10gDialect

#Added for jwt secret purposes
bezkoder.app.jwtSecret= bezKoderSecretKey
bezkoder.app.jwtExpirationMs= 1800000

server.servlet.session.cookie.same-site=none

# Save the database to a file - it will be placed in the project folder
#spring.datasource.url=jdbc:h2:file:./database

# Drop and Create the database at each start
#spring.jpa.hibernate.ddl-auto=create-drop

# Load the database
#spring.jpa.hibernate.ddl-auto=update

# App Properties
server.port=8080
